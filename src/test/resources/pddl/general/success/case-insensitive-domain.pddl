(DefIne (domAin tEst)
(:requirements :strips :tyPing)
  (:types
  	tesTtyPe tesTtype1 - tEstTYPE2
  	Testtype2)
  (:constants testConst - tEstconst2
              tesTconst2)
(:Predicates (teSt1 ?X) (teSt2 ?X - TESTtype) (teSt3 ?X - (either TESTtype1 TeSTTYPE2)))

(:action tEst
  :parameters  (?X)
  :precondition (TEst ?x)
  :effect ()))
